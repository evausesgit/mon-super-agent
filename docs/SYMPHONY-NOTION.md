# Symphony + Notion (moved)

The Notion-driven task orchestrator that this document used to describe was extracted to its own repository:

<https://github.com/evausesgit/notion-orchestrator>

That project contains:

- the canonical Notion database schema (`docs/notion-setup.md`)
- the configuration reference (`docs/configuration.md`)
- the Docker image (`ghcr.io/evausesgit/notion-orchestrator`)

If you arrived here from a tracker link, follow the URL above for current documentation.
